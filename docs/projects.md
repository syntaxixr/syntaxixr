# Projects

- Repo cards (Receipts first, then OrbSniper): [socialify](https://socialify.git.ci), theme Dark,
  pattern Circuit Board, shows description, language, owner and stars.
- Sites: screenshots live in `assets/` (`klassgo.jpg`, `zhizntemy.jpg`), two-column table.

To add a site: put a screenshot into `assets/`, add a `<td>` with link and one-line description.
