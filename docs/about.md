# About me

Written as a `ts` code block so GitHub highlights it.
Keep keys aligned to the same column — it reads like a config, which is the point.
Bullets under the block are the short human version.
