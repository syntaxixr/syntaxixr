# AI copilots

shields.io badges, colors step from `bf00ff` down to `3b0764` so the row reads as one gradient.
Logos come from [Simple Icons](https://simpleicons.org); if a logo is missing the badge
falls back to a generic one (`probot` for GLM).
